import React from 'react';
import { Text } from '@chakra-ui/react';
const ApplicationCompleted = () => {
    return (
        <div>
            <Text>You have completed your application</Text>
        </div>
    );
};

export default ApplicationCompleted;
